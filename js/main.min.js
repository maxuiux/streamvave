/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./src/js/import/input-add-nickname.js":
/*!*********************************************!*\
  !*** ./src/js/import/input-add-nickname.js ***!
  \*********************************************/
/***/ (() => {

var blockForm = document.querySelector('.add-nickname');

if (blockForm) {
  // Функция для валидации input
  var validateInput = function validateInput(input) {
    if (input === '') {
      // Если поле пустое, удалить оба класса
      inputField.classList.remove('input-true', 'input-false');
      smileTrue.classList.remove(classActive);
      smileFalse.classList.remove(classActive);
      smileStatic.classList.add(classActive);
      btnSumbitNick.setAttribute('data-disabled', 'true');
    } else if (regex.test(input)) {
      // Если ввод соответствует правилам
      inputField.classList.add('input-true');
      inputField.classList.remove('input-false');
      smileTrue.classList.add(classActive);
      smileFalse.classList.remove(classActive);
      smileStatic.classList.remove(classActive); // Активируем кнопку при неправильном вводе

      btnSumbitNick.setAttribute('data-disabled', 'false');
    } else {
      // Если ввод не соответствует правилам
      inputField.classList.add('input-false');
      inputField.classList.remove('input-true');
      smileFalse.classList.add(classActive);
      smileTrue.classList.remove(classActive);
      smileStatic.classList.remove(classActive); // Де активируем кнопку при неправильном вводе

      btnSumbitNick.setAttribute('data-disabled', 'true');
    }

    inputField.value = input.toLowerCase();
  }; // Пример использования функции при изменении значения в поле input


  var addUrl = function addUrl() {
    // Получаем значение введенное в input
    var dataInput = inputField.value; // Получаем элемент iframe по его id

    var iframe = document.getElementById('stream'); // Формируем новый URL адрес с данными из input

    var newUrl = "https://player.twitch.tv/?channel=".concat(dataInput, "&parent=maxuiux.github.io"); // Устанавливаем новый URL в src атрибут iframe

    iframe.src = newUrl;
    var inputData = inputField.value; // Добавляем данные в адресную строку

    history.pushState(null, null, '?data=' + encodeURIComponent(inputData)); // Сохраняем данные в localStorage

    localStorage.setItem('data', inputData); // данные в title

    var currentTitle = document.title;

    if (currentTitle.includes(' - ')) {
      // Если содержит, заменяем старую информацию на новую
      var newTitle = inputData + ' - ' + 'StreamVave';
      document.title = newTitle;
    } else {
      // Если не содержит, просто добавляем новую информацию
      document.title = inputData + ' - ' + 'StreamVave';
    }

    modal.setAttribute(dataModalActive, dataFalse);
  };

  var modal = document.querySelector('.modal');
  var regex = /^[a-zA-Z0-9_]+$/;
  var inputField = document.getElementById('input-nick');
  var smileTrue = document.getElementById('input-icon-true');
  var smileFalse = document.getElementById('input-icon-false');
  var smileStatic = document.getElementById('input-icon-static');
  var btnSumbitNick = document.getElementById('btn-submit-nick');
  var classActive = 'active';
  var dataModalActive = 'data-modal-active';
  var dataFalse = 'false'; // Получаем параметры из URL

  var urlParams = new URLSearchParams(window.location.search);
  var inputDataFromUrl = urlParams.get('data');
  document.getElementById('input-nick').addEventListener('input', function () {
    validateInput(this.value);
  });
  inputField.addEventListener('mouseover', function () {
    smileStatic.classList.add('hover');
  });
  inputField.addEventListener('mouseout', function () {
    smileStatic.classList.remove('hover');
  });
  inputField.addEventListener('focus', function () {
    smileStatic.classList.add('focus');
  });
  inputField.addEventListener('blur', function () {
    smileStatic.classList.remove('focus');
  });
  btnSumbitNick.addEventListener('click', function () {
    addUrl();
  });
  window.addEventListener('keydown', function (event) {
    if (event.defaultPrevented) {
      return; // Не делаем ничего, если действие по умолчанию было отменено
    }

    var handled = false;

    if (event.key === 'Enter') {
      // Обрабатываем событие для клавиши Enter
      handled = true;
      addUrl(); // Вызываем функцию при нажатии на Enter
    }

    if (event.key === 'Escape') {
      handled = true;
      modal.classList.remove(classActive);
    }

    if (handled) {
      // Предотвращаем "двойное действие", если событие обработано
      event.preventDefault();
    }
  }, true); // Используем данные из адресной строки

  if (inputDataFromUrl) {
    // Получаем элемент iframe по его id
    var iframe = document.getElementById('stream');
    inputField.value = inputDataFromUrl; // Меняем название в title

    document.title = inputDataFromUrl + ' - ' + document.title; // Формируем новый URL адрес с данными из input

    var newUrl = "https://player.twitch.tv/?channel=".concat(inputDataFromUrl, "&parent=maxuiux.github.io"); // Устанавливаем новый URL в src атрибут iframe

    iframe.src = newUrl;
  }
}

/***/ }),

/***/ "./src/js/import/localstorage-input-checkbox-radio.js":
/*!************************************************************!*\
  !*** ./src/js/import/localstorage-input-checkbox-radio.js ***!
  \************************************************************/
/***/ (() => {

var blockContent = document.querySelector('.navigation__content');

if (blockContent) {
  // Получаем все чекбоксы с классом .switch-btn
  var checkboxes = document.querySelectorAll('.switch-btn-full-screen, .switch-btn-cinema-mode, .switch-btn-simplified-chat'); // Перебираем каждый чекбокс

  checkboxes.forEach(function (checkbox) {
    // Добавляем обработчик события change
    checkbox.addEventListener('change', function () {
      // Сохраняем состояние чекбокса в localStorage
      localStorage.setItem('checkbox_' + this.id, this.checked);
    }); // Проверяем значение в localStorage и устанавливаем его

    var checked = localStorage.getItem('checkbox_' + checkbox.id) === 'true';
    checkbox.checked = checked;
  }); // Получаем все радио-кнопки с классом .radio-btn и атрибутом

  var radioButtons = document.querySelectorAll('.radio-btn-arrangement-element, .radio-btn-width-container'); // Получаем все радио-кнопки с классом .radio-btn и атрибутом

  var radioButtonsArrangementElement = document.querySelectorAll('.radio-btn-arrangement-element'); // Получаем все радио-кнопки с классом .radio-btn и атрибутом

  var radioButtonsWidthContainer = document.querySelectorAll('.radio-btn-width-container'); // Проверяем, есть ли радио-кнопки в группе

  if (radioButtonsArrangementElement.length > 0) {
    // Устанавливаем атрибут checked на первую радио-кнопку
    radioButtonsArrangementElement[0].checked = true;
  } // Проверяем, есть ли радио-кнопки в группе


  if (radioButtonsWidthContainer.length > 0) {
    // Устанавливаем атрибут checked на первую радио-кнопку
    radioButtonsWidthContainer[0].checked = true;
  } // Перебираем каждую радио-кнопку


  radioButtons.forEach(function (radioButton) {
    // Добавляем обработчик события change
    radioButton.addEventListener('change', function () {
      // Сохраняем состояние выбранной радио-кнопки в localStorage
      localStorage.setItem('radio_' + this.name, this.value);
    }); // Проверяем значение в localStorage и устанавливаем его

    var checkedValue = localStorage.getItem('radio_' + radioButton.name);

    if (checkedValue === radioButton.value) {
      radioButton.checked = true;
    }
  });
}

/***/ }),

/***/ "./src/js/import/modal-add-nickname.js":
/*!*********************************************!*\
  !*** ./src/js/import/modal-add-nickname.js ***!
  \*********************************************/
/***/ (() => {

var blockModal = document.querySelector('.modal__add-nickname');

if (blockModal) {
  var modals = document.querySelectorAll('.modal'); // class blocks

  var body = document.querySelector('body');
  var modal = document.querySelector('.modal__add-nickname');
  var btnClose = document.querySelector('.modal-close-btn__add-nickname');
  var btnOpenModal = document.querySelector('.header__btn'); // add class

  var overflow = 'overflow';
  var dataModalActive = 'data-modal-active';
  var dataTrue = 'true';
  var dataFalse = 'false';
  btnOpenModal.addEventListener('click', function () {
    modal.setAttribute(dataModalActive, dataTrue);
    body.classList.add(overflow);
  });
  btnClose.addEventListener('click', function () {
    modal.setAttribute(dataModalActive, dataFalse);
    body.classList.remove(overflow);
  });
  modals.forEach(function (modal) {
    modal.onclick = function (event) {
      if (event.target == modal) {
        modal.setAttribute(dataModalActive, dataFalse);
        body.classList.remove(overflow);
      }
    };
  });
}

/***/ }),

/***/ "./src/js/import/modal-navigation.js":
/*!*******************************************!*\
  !*** ./src/js/import/modal-navigation.js ***!
  \*******************************************/
/***/ (() => {

var blockModal = document.querySelector('.modal__nav');

if (blockModal) {
  // class blocks
  var body = document.querySelector('body');
  var modal = document.querySelector('.modal__nav');
  var btnClose = document.querySelector('.modal-close-btn__nav');
  var btnOpenModal = document.querySelector('.stream__btn'); // add class

  var overflow = 'overflow';
  var dataModalActive = 'data-modal-active';
  var dataTrue = 'true';
  var dataFalse = 'false'; // modal.setAttribute(dataModalActive, dataTrue)

  btnOpenModal.addEventListener('click', function () {
    modal.setAttribute(dataModalActive, dataTrue);
    body.classList.add(overflow);
  });
  btnClose.addEventListener('click', function () {
    modal.setAttribute(dataModalActive, dataFalse);
    body.classList.remove(overflow);
  });
}

/***/ }),

/***/ "./src/js/import/modal.js":
/*!********************************!*\
  !*** ./src/js/import/modal.js ***!
  \********************************/
/***/ (() => {



/***/ }),

/***/ "./src/js/import/navigation.js":
/*!*************************************!*\
  !*** ./src/js/import/navigation.js ***!
  \*************************************/
/***/ (() => {

var blockNavigation = document.querySelector('.navigation');

if (blockNavigation) {
  var itemSections = document.querySelectorAll('.navigation__wrap');

  if (itemSections.length > 0) {
    itemSections.forEach(function (item) {
      var tabBtns = item.querySelectorAll('.js-tab-button');
      var tabContent = item.querySelectorAll('.js-tab-content');
      tabContent.forEach(function (content) {
        content.classList.add('noactive');
        content.classList.remove('block');
      });
      tabBtns.forEach(function (btn, index) {
        btn.addEventListener('click', function (event) {
          tabBtns.forEach(function (btn) {
            btn.classList.remove('active');
          });
          tabContent.forEach(function (content) {
            content.classList.add('noactive');
            content.classList.remove('block');
          });
          event.target.classList.add('active');
          item.querySelector("[data-content=\"".concat(event.target.dataset.tab, "\"]")).classList.remove('noactive');
          item.querySelector("[data-content=\"".concat(event.target.dataset.tab, "\"]")).classList.add('block');
        });
        index == 0 ? btn.click() : '';
      });
    });
  }
}

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be in strict mode.
(() => {
"use strict";
/*!*************************!*\
  !*** ./src/js/index.js ***!
  \*************************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _import_modal__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./import/modal */ "./src/js/import/modal.js");
/* harmony import */ var _import_modal__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_import_modal__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _import_modal_add_nickname__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./import/modal-add-nickname */ "./src/js/import/modal-add-nickname.js");
/* harmony import */ var _import_modal_add_nickname__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_import_modal_add_nickname__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _import_input_add_nickname__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./import/input-add-nickname */ "./src/js/import/input-add-nickname.js");
/* harmony import */ var _import_input_add_nickname__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_import_input_add_nickname__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _import_navigation__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./import/navigation */ "./src/js/import/navigation.js");
/* harmony import */ var _import_navigation__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_import_navigation__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var _import_modal_navigation__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./import/modal-navigation */ "./src/js/import/modal-navigation.js");
/* harmony import */ var _import_modal_navigation__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(_import_modal_navigation__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var _import_localstorage_input_checkbox_radio__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./import/localstorage-input-checkbox-radio */ "./src/js/import/localstorage-input-checkbox-radio.js");
/* harmony import */ var _import_localstorage_input_checkbox_radio__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(_import_localstorage_input_checkbox_radio__WEBPACK_IMPORTED_MODULE_5__);






})();

/******/ })()
;
//# sourceMappingURL=main.js.map